import { Container, Typography, Button } from "@mui/material"

export const CuratorView = () => {
  return (
    <Container>
      <Typography> This is the Curator's View </Typography>
      <Button> Add Citation </Button>
    </Container>
  )
}
