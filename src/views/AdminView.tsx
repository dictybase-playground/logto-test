import { Container, Typography, Button } from "@mui/material"

export const AdminView = () => {
  return (
    <Container>
      <Typography> This is the Administrator's View </Typography>
      <Button> Add Citation </Button>
      <Button> Edit this Page </Button>
    </Container>
  )
}
